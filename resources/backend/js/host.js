if ($('#host-page').length > 0) {
    const app = new Vue({
        el: '#host-page',
        data: {
            type: 'backends/host',
        },
        mounted: function(){
        },
        methods: {
            
        }
    });
  }
  
